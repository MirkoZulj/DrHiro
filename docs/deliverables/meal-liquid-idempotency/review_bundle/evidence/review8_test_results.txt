# Review round 8 evidence - focused corrections

Generated: 2026-09-11T07:22:44Z (UTC)

## Environment

```
host:            Linux 6.8.0-1060-raspi aarch64
os:              Ubuntu 24.04.4 LTS
python:          Python 3.12.3
docker:          Docker version 29.5.3, build d1c06ef
compose:         5.1.4
postgres:        PostgreSQL 16.14 (Debian 16.14-1.pgdg13+1) on aarch64-unknown-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit
server_version:  16.14 (Debian 16.14-1.pgdg13+1)
sqlalchemy:      2.0.52
psycopg2:        2.9.12 (dt dec pq3 ext lo64)
branch:          feature/meal-liquid-idempotency
functional base: d46ed10bde10bd415a06f943c6eff5e6ce526ae0
functional head: 1b7f7523bffaa7386b5cdcd1e7ecb698388d6662
docs commit:     see BASE_AND_HEAD.md in the bundle (self-reference)
frozen cand.:    7e2cf6915dbd7478e8a558817d4d51aa63879e60 (unchanged)
```

## Source actually imported (rerun discipline)

drhiro_api is an EDITABLE install pointing at /home/mirko/work/DrHiro/apps/api/src. A before/after
run inside a worktree therefore imports the MAIN tree unless PYTHONPATH is forced.
These are the paths and hashes actually used:

```
--- HOST (fixed source, at head) ---
resolved: /home/mirko/work/DrHiro/apps/api/src/drhiro_api/schema_activities.py
host  e1be72e20dfae8c14f8b2a1726ecf7ae037f6611a262aeb2c105352ee9ce9cc9  /home/mirko/work/DrHiro/apps/api/src/drhiro_api/schema_activities.py
host  115d23be630919c5108ab9542b5bffc42fe31bc3d008cc13aeb60faba5fe3ab5  /home/mirko/work/DrHiro/deploy/disposable/app/ingress.py

--- PRE-FIX WORKTREE (base source, forced via PYTHONPATH) ---
worktree: d46ed10bde10bd415a06f943c6eff5e6ce526ae0
resolved: /tmp/review8-before/apps/api/src/drhiro_api/schema_activities.py
fix markers present (must all be False for a true pre-fix run):
  index_definitions: False
  _canon_default   : False
  column_types     : False
bef   6319b8faa1241d72309bfa213c2c26c22b30767ad38310bfa96243c29a98e0cb  /tmp/review8-before/apps/api/src/drhiro_api/schema_activities.py
```

## Exact commands

```bash
# migration: FAILING-BEFORE (base worktree, PRE-FIX source forced via PYTHONPATH)
cd /tmp/review8-before
PYTHONPATH=/tmp/review8-before/apps/api/src DRHIRO_ACTIVITIES_MIGRATION_DB=1 \
  DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' python -m pytest \
  tests/test_r4_activities_migration.py::TestDefinitionEquivalenceIsConservative -q

# migration: PASSING-AFTER (head source)
cd /home/mirko/work/DrHiro
DRHIRO_ACTIVITIES_MIGRATION_DB=1 DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' \
  python -m pytest tests/test_r4_activities_migration.py -q

# ingress regression suite (requires the disposable stack UP)
DRHIRO_ISOLATED_STACK=1 python -m pytest tests/test_t1_isolated_ingress_stack.py -q

# default suite, production inspection excluded
env -u DRHIRO_ISOLATED_STACK -u DRHIRO_R1R2_ALEMBIC_DB -u DRHIRO_R4_ALEMBIC_DB \
  -u DRHIRO_T1_ALEMBIC_DB -u DRHIRO_ACTIVITIES_MIGRATION_DB -u DRHIRO_DEPLOY_ASSERT \
  -u DRHIRO_DOCKER_CMD -u DRHIRO_TEST_DB_URL -u REDIS_URL python -m pytest tests/ -q
```

## B6 - definition equivalence: FAILING-BEFORE / PASSING-AFTER

### FAILING-BEFORE - new negative tests against the PRE-FIX validator

```
E       AssertionError: timestamp without time zone was accepted: fatal=[]
E       assert False
E        +  where False = any(<generator object TestDefinitionEquivalenceIsConservative.test_timestamp_without_time_zone_is_fatal.<locals>.<genexpr> at 0xffff924d58a0>)

tests/test_r4_activities_migration.py:503: AssertionError
=========================== short test summary info ============================
FAILED tests/test_r4_activities_migration.py::TestDefinitionEquivalenceIsConservative::test_modified_default_expression_is_fatal
FAILED tests/test_r4_activities_migration.py::TestDefinitionEquivalenceIsConservative::test_partial_index_is_fatal
FAILED tests/test_r4_activities_migration.py::TestDefinitionEquivalenceIsConservative::test_unique_index_is_fatal
FAILED tests/test_r4_activities_migration.py::TestDefinitionEquivalenceIsConservative::test_foreign_key_to_same_name_in_other_schema_is_fatal
FAILED tests/test_r4_activities_migration.py::TestDefinitionEquivalenceIsConservative::test_timestamp_without_time_zone_is_fatal
5 failed, 1 passed in 2.05s
```

Each of the four cases the review reproduced fails pre-fix; the baseline
(unaltered production shape) passes, so the tests cannot pass by rejecting
everything.

### PASSING-AFTER - full R4 suite against the FIXED validator

```
........................                                                 [100%]
24 passed in 2.06s
```

## Ingress regression suite (disposable stack; captured while RUNNING)

The stack is stopped after capture; it is not restarted by this script.

Classes covering the review findings:
  TestDuplicateReceiptHandling     - B1 cursor contract, stored-text re-drive,
                                     retry budget (redelivery + lease recovery)
  TestResendStrictConfirmation     - strict literal-true confirmation
  TestAttemptFencingInFlight       - B4 late result WHILE A NEWER ATTEMPT IS
                                     IN FLIGHT (delivery + explicit resend)

```
=== REVIEW ROUND 8 stack suite ===
.....................................................                    [100%]
53 passed in 747.21s (0:12:27)
```

## Default suite (production inspection excluded)

```
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
363 passed, 123 skipped, 55 warnings in 42.29s
```

## B7 - status

POLLING OFFSET: IMPLEMENTED in this round. Acknowledgement is no longer a bespoke
/ack call - it uses the real Bot API offset contract (getUpdates(offset=N)), with
the offset persisted durably in telegram_consumer_offset and written AFTER
processing so a crash re-fetches rather than skips.

SENDER IDENTITY: still UNIMPLEMENTED. A verified bot identity authenticates the
bot, not the sender; every message maps to one fixed disposable user.

EDITS / CONTENT CONFLICTS: still UNIMPLEMENTED. An edited message_id is treated as
a duplicate receipt, not a revision.

## Capability labelling

Container uptimes unchanged across the session support 'no observed restart'.
They do NOT independently prove that configuration, credentials or data were
unchanged. The evidence for 'no production change' is that every action targeted
the disposable stack or a disposable test database, with production read-only.

The passing disposable-stack tests are EVIDENCE, not production acceptance.
