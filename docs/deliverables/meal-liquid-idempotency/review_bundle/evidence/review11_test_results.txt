# Review round 10 evidence - focused FK mapping + schema policy

Generated: 2026-09-11T13:22:52Z (UTC)

## Environment

```
host:            Linux 6.8.0-1060-raspi aarch64
os:              Ubuntu 24.04.4 LTS
python:          Python 3.12.3
docker:          Docker version 29.5.3, build d1c06ef
compose:         5.1.4
postgres:        PostgreSQL 16.14 (Debian 16.14-1.pgdg13+1) on aarch64-unknown-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit
branch:          feature/meal-liquid-idempotency
functional base: 74ca71f8dec9368c3274b2166a5d9b19d3b94a2b
functional head: 064e373
docs head (capture-time revision): 064e3736792f3c2d9e5f8834a87de322a091bffb
  NOTE: this is the repository revision AT CAPTURE TIME. The evidence file 
  records its own commit, so committing it necessarily creates a NEWER docs
  head; BASE_AND_HEAD.md in the bundle names that final one. The two are not
  the same revision by construction and must not be presented as such.
frozen cand.:    7e2cf6915dbd7478e8a558817d4d51aa63879e60 (unchanged)
```

## Source actually imported (rerun discipline)

drhiro_api is an EDITABLE install pointing at /home/mirko/work/DrHiro/apps/api/src; a before/after
run inside a worktree imports the MAIN tree unless PYTHONPATH is forced. These are
the paths and hashes actually used:

```
--- HOST (fixed source, at head) ---
resolved: /home/mirko/work/DrHiro/apps/api/src/drhiro_api/schema_activities.py
host  8fc55d81dff1674dcef607a9f7ed0f6e9e238b705b9ea7153d0c27884e65c57f  /home/mirko/work/DrHiro/apps/api/src/drhiro_api/schema_activities.py
host  a5d33b2a863232e58a1036fa626d3f690fbf55fc76f6267dff871c3dfc4b1d56  /home/mirko/work/DrHiro/deploy/disposable/app/ingress.py

--- RUNNING INGRESS CONTAINER (what the image actually executed) ---
ctnr  service "ingress" is not running

--- PRE-FIX WORKTREE (base source, forced via PYTHONPATH) ---
worktree: 74ca71f8dec9368c3274b2166a5d9b19d3b94a2b
resolved: /tmp/review11-before/apps/api/src/drhiro_api/schema_activities.py
fix markers present (must be False for a true pre-fix run):
  _relation_oid : True
  _type_precision: True
  column_types(oid) len guard: False
bef   c375ba78f23e5f7ba2f5a8f345a1c15e77e5cd18a8169b705fd93868650177a8  /tmp/review11-before/apps/api/src/drhiro_api/schema_activities.py
```

## Exact commands

Every command below is printed beside the result it produced in this capture.

```bash
# 1. migration FAILING-BEFORE (base worktree = fully-reviewed round-10 state;
#    PRE-FIX source forced via PYTHONPATH so the editable install cannot
#    substitute the fixed source, with the imported path printed)
cd /tmp/review11-before && PYTHONPATH=/tmp/review11-before/apps/api/src python -c \
  'import drhiro_api.schema_activities as m; print(m.__file__)'
PYTHONPATH=/tmp/review11-before/apps/api/src DRHIRO_ACTIVITIES_MIGRATION_DB=1 \
  DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' python -m pytest \
  tests/test_r4_activities_migration.py::TestSchemaNoneDoesNotCombineRelations \
  tests/test_r4_activities_migration.py::TestConservativeTypeAndDefaultEquivalence -q

# 2. migration FAILING-BEFORE for the ROUND-9 validator (FK mapping + schema policy)
cd /tmp/review11-r9 && PYTHONPATH=/tmp/review11-r9/apps/api/src python -c \
  'import drhiro_api.schema_activities as m; print(m.__file__)'
PYTHONPATH=/tmp/review11-r9/apps/api/src DRHIRO_ACTIVITIES_MIGRATION_DB=1 \
  DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' python -m pytest \
  tests/test_r4_activities_migration.py::TestForeignKeyMappingAndSchemaPolicy -q

# 3. migration FAILING-BEFORE for the ROUND-10 creation path (default-schema gap)
cd /tmp/review11-r10 && PYTHONPATH=/tmp/review11-r10/apps/api/src python -c \
  'import drhiro_api.schema_activities as m; print(m.__file__)'
/home/mirko/work/DrHiro/.venv/bin/python /home/mirko/work/DrHiro/deploy/disposable/creation_path_probe.py /tmp/review11-r10

# 4. migration PASSING-AFTER - full R4 suite plus the creation-path probe
cd /home/mirko/work/DrHiro
DRHIRO_ACTIVITIES_MIGRATION_DB=1 DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' \
  python -m pytest tests/test_r4_activities_migration.py -q
/home/mirko/work/DrHiro/.venv/bin/python /home/mirko/work/DrHiro/deploy/disposable/creation_path_probe.py /home/mirko/work/DrHiro

# 5. ingress regression suite (requires the disposable stack UP)
DRHIRO_ISOLATED_STACK=1 python -m pytest tests/test_t1_isolated_ingress_stack.py -q

# 6. default suite (production inspection excluded)
env -u DRHIRO_ISOLATED_STACK -u DRHIRO_ACTIVITIES_MIGRATION_DB -u DRHIRO_TEST_DB_URL \
  python -m pytest tests/ -q
```

## Finding 2 + 3 - failing-before / passing-after (real PostgreSQL)

### FAILING-BEFORE - new negatives against the PRE-FIX validator

```
........                                                                 [100%]
8 passed in 1.00s
```

## Finding: FK mapping + schema policy - failing-before against the ROUND-9 validator

The round-9 validator dropped the referenced-COLUMN comparison and resolved
`users` independently through search_path. Both regressions below fail against
the round-9 validator (commit 9076937) and pass after the correction.

```
E             Left contains one more item: 'foreign key activities_user_id_fkey refers to second.users (oid 126251); expected the users relation in the activities schema (oid 126245)'
E             Use -v to get more diff

tests/test_r4_activities_migration.py:774: AssertionError
=========================== short test summary info ============================
FAILED tests/test_r4_activities_migration.py::TestForeignKeyMappingAndSchemaPolicy::test_referenced_column_alternate_id_is_fatal
FAILED tests/test_r4_activities_migration.py::TestForeignKeyMappingAndSchemaPolicy::test_users_only_leading_schema_is_not_used
2 failed, 1 passed in 0.65s
```

## Creation path - the NEW negative against the ROUND-10 code

The regression the review asked for, executed against the submitted round-10
worktree (the imported source path is printed first):

```
imported: /tmp/review11-r10/apps/api/src/drhiro_api/schema_activities.py
E               AttributeError: module 'drhiro_api.schema_activities' has no attribute 'MissingUsersRelation'

tests/test_r4_activities_migration.py:877: AttributeError
=========================== short test summary info ============================
FAILED tests/test_r4_activities_migration.py::TestCreationDestinationNamespace::test_default_schema_creation_refuses_cross_schema_users
1 failed, 2 passed in 0.71s
```

## Creation path - failing-before against the ROUND-10 code

With schema=None the round-10 create_activities emitted an UNQUALIFIED
REFERENCES users(id), so under search_path = first, second (first writable,
no users; second has users) it created first.activities bound to second.users
- the cross-schema relationship the validator rejects. The probe reports the
outcome and the surviving tables/indexes.

```
imported: /tmp/review11-r10/apps/api/src/drhiro_api/schema_activities.py
RESULT: create_activities SUCCEEDED  <-- policy violation
STATE: created first.activities referencing second.users
STATE: activities tables in first/second = 1; indexes = 3
```

### PASSING-AFTER - creation path refuses, and the full R4 suite passes

```
imported: /home/mirko/work/DrHiro/apps/api/src/drhiro_api/schema_activities.py
RESULT: refused with MissingUsersRelation: refusing to create first.activities: that schema has no `users` relation, and the same-schema policy forbids referencing another schema's users
STATE: activities tables in first/second = 0; indexes = 0
```

### PASSING-AFTER - full R4 suite against the FIXED validator

```
......................................                                   [100%]
38 passed in 3.36s
```

## Ingress regression suite (disposable stack; captured while RUNNING)

Finding 1 coverage (TestExhaustionSweepRespectsActiveLease):
  - live final claim survives a concurrent recovery pass (status/token/lease
    intact; exhausted=0); worker released -> exactly one consumption + completion
  - expired final claim is swept to exhausted and refuses redelivery

```
.......................................................                  [100%]
55 passed in 797.39s (0:13:17)
```

## B7 - status

SENDER IDENTITY: still UNIMPLEMENTED. A verified bot identity authenticates the
bot, not the sender; every message maps to one fixed disposable user.

EDITS / CONTENT CONFLICTS: still UNIMPLEMENTED. An edited message_id is treated as
a duplicate receipt, not a revision.

POLLING OFFSET: implemented (review 8), real Bot API offset contract with a durable
offset in telegram_consumer_offset. Not full production polling acceptance.

## Carry-forward justification for the ingress stack suite

ingress.py is UNCHANGED in this round, so the stack-suite total and the
container hash below are the round-10 ones and still describe the submitted
head. Verified by Git BLOB identity (not by commit label):

```
base 74ca71f8dec9368c3274b2166a5d9b19d3b94a2b:ingress.py  d183a1463f14402f0f511c5597eb4c84f28525a9
working tree ingress.py     d183a1463f14402f0f511c5597eb4c84f28525a9
```

A differing blob would invalidate the carry-forward; identical blobs mean the
same source was exercised. The stack suite itself was NOT re-run this round.

## Default suite (production inspection excluded; captured in this run)

```
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
363 passed, 139 skipped, 55 warnings in 41.14s
```

## Capability labelling

Container uptimes unchanged support 'no observed restart' only; they do not
independently prove that configuration, credentials or data were unchanged. Every
action targeted the disposable stack or a disposable test database. Passing
disposable-stack tests are EVIDENCE, not production acceptance.
