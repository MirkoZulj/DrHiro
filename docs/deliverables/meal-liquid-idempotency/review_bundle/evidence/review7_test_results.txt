# Review #7 evidence — blocker fixes on the isolated branch

Generated: 2026-09-10T21:39:55Z (UTC)

## Environment

```
host:            Linux 6.8.0-1060-raspi aarch64
os:              Ubuntu 24.04.4 LTS
python:          Python 3.12.3
docker:          Docker version 29.5.3, build d1c06ef
compose:         5.1.4
postgres:        PostgreSQL 16.14 (Debian 16.14-1.pgdg13+1) on aarch64-unknown-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit
server_version:  16.14 (Debian 16.14-1.pgdg13+1)
sqlalchemy:      2.0.52
psycopg2:        2.9.12 (dt dec pq3 ext lo64)
branch:          feature/meal-liquid-idempotency
base:            8921c305bce543f38653a5a948018a6ba33ba997
head:            8ea0bcbced78b0f2c1a210047873779cc9008577
frozen cand.:    7e2cf6915dbd7478e8a558817d4d51aa63879e60 (unchanged)
```

Note on capability labelling: container uptimes were unchanged across the session.
That supports **"no observed restart"**. It does NOT independently prove that
configuration, credentials, or data were unchanged. The evidence for 'no
production change' is that every action targeted the disposable stack (drhiro-iso)
or a disposable test database, and production was consulted read-only.

## Exact commands

```bash
# migration: FAILING-BEFORE (clean worktree of the base, PRE-FIX source forced via
# PYTHONPATH because drhiro_api is an editable install pointing at the main tree -
# without this the pre-fix run silently imports the FIXED code and passes)
cd /tmp/review7-before
PYTHONPATH=/tmp/review7-before/apps/api/src DRHIRO_ACTIVITIES_MIGRATION_DB=1 \
  DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' python -m pytest \
  tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas -q

# migration: PASSING-AFTER (HEAD source)
cd /home/mirko/work/DrHiro
DRHIRO_ACTIVITIES_MIGRATION_DB=1 DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_test' \
  python -m pytest tests/test_r4_activities_migration.py -q

# disposable stack suite (requires the stack UP; captured before the instructed stop)
DRHIRO_ISOLATED_STACK=1 python -m pytest tests/test_t1_isolated_ingress_stack.py -q

# default suite, production inspection excluded
env -u DRHIRO_ISOLATED_STACK -u DRHIRO_R1R2_ALEMBIC_DB -u DRHIRO_R4_ALEMBIC_DB \
  -u DRHIRO_T1_ALEMBIC_DB -u DRHIRO_ACTIVITIES_MIGRATION_DB -u DRHIRO_DEPLOY_ASSERT \
  -u DRHIRO_DOCKER_CMD -u DRHIRO_TEST_DB_URL -u REDIS_URL python -m pytest tests/ -q

# R1/R2 gated
DRHIRO_R1R2_ALEMBIC_DB=1 DRHIRO_R4_ALEMBIC_DB=1 DRHIRO_TEST_DB_URL='postgresql+psycopg2://drhiro:drhiro@localhost:5435/drhiro_r1r4_alembic' \
  REDIS_URL='redis://localhost:6382/15' python -m pytest \
  tests/test_r4_orm_alembic_parity.py tests/test_r4_migration_chain.py \
  tests/test_r1_nutrition_resolution.py tests/test_r2_trusted_key_set.py \
  tests/test_r1_stack_isolation.py -q
```

## B6 — migration adoption validator: FAILING-BEFORE / PASSING-AFTER

### Proof the pre-fix run really used the PRE-FIX source

```
schema_activities from: /tmp/review7-before/apps/api/src/drhiro_api/schema_activities.py
_canon_check_expr (fix marker) present: False
EXPECTED_DEFAULTS (fix marker) present: False
```

### FAILING-BEFORE — the 7 new negative tests against the PRE-FIX validator (base 8921c305bce543f38653a5a948018a6ba33ba997)

```
E       AssertionError: []
E       assert False
E        +  where False = any(<generator object TestAdoptionRejectsInvalidSchemas.test_missing_server_defaults_are_fatal.<locals>.<genexpr> at 0xffffb6752400>)

tests/test_r4_activities_migration.py:400: AssertionError
=========================== short test summary info ============================
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_check_with_wrong_lower_bound_is_fatal
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_wrong_index_columns_are_fatal_not_reconciled
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_missing_pk_is_fatal
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_wrong_pk_columns_are_fatal
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_missing_fk_is_fatal
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_wrong_fk_target_is_fatal
FAILED tests/test_r4_activities_migration.py::TestAdoptionRejectsInvalidSchemas::test_missing_server_defaults_are_fatal
7 failed in 0.81s
```

### PASSING-AFTER — full R4 suite against the FIXED validator (head 8ea0bcbced78b0f2c1a210047873779cc9008577)

```
..................                                                       [100%]
18 passed in 1.47s
```

## Disposable stack suite (captured while the stack was RUNNING)

The stack was stopped on instruction after this capture and is NOT restarted here.

```
=== REVIEW #7 stack suite ===
.....................................                                    [100%]
37 passed in 413.13s (0:06:53)
```

Recorded verbatim from the capture made while the stack was running (preserved in
the git history, commit 42fe5ba). The stack could not be re-run for this evidence
because it was stopped on instruction. NOTE: the earlier capture of that same
suite, made before the concurrent-probe isolation fix, showed 36 passed / 1 failed
(conflicting_payload_reuse); the 37-passed run is the post-fix result.


Independent checks run against the live stack before the stop:

```
from the model-accessible 'openclaw' container:
  GET http://ingress:8082/admin/status  -> UNREACHABLE (URLError)   [B2: network-level denial]
from the trusted ingress loopback, NO Authorization header:
  GET /admin/status -> HTTP 401 {"error":"unauthorised"}          [B2: auth enforced on GET]
```

## Default suite (production inspection excluded)

```
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
363 passed, 101 skipped, 55 warnings in 41.55s
```

## R1/R2 gated

```
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
49 passed, 5 warnings in 2.36s
```

## B7 — status: NOT CLOSED (wording corrected, behaviour unimplemented)

Real polling-offset, sender-identity resolution, and edit/content-conflict
handling are UNIMPLEMENTED. No code path implements any of the three. A verified
bot identity authenticates the bot, not the sender.

## Attempt-fencing coverage (precise)

COVERED by TestAttemptFencing (2 tests), both injecting a delayed completion from
an old attempt id:
  (1) late old result arriving AFTER an acknowledgement;
  (2) late old result arriving AFTER the transition to unknown.

NOT COVERED: a late old result arriving WHILE A NEWER ATTEMPT IS ACTIVELY IN
FLIGHT (state in_flight with current_attempt_id set to a different, newer attempt
id). No test exercises that interleaving. The implementation guards it by
construction (completions filter on current_attempt_id = the completing attempt),
but that is a code-reading claim, not a verified one. Coverage gap.
